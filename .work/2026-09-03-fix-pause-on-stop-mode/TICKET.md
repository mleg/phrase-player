# UX bug in after phrase mode "stop"

When in "After phrase" mode "stop" a right arrow is pressed, the next phrase should play automatically.
But only if the the current pause was done on its own upon reaching the end of the current phrase, not by the user's actions
